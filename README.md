# 🤖 AI Fitness Coach

Персональный AI-ассистент для построения здорового образа жизни, который объединяет тренировки, питание и восстановление.

## ✨ Основные возможности

### 🎯 Персонализация
- **Индивидуальный подход**: планы создаются на основе возраста, веса, роста, целей и уровня активности
- **AI-анализ**: использует LLaMA 3.3 70B для генерации детальных рекомендаций
- **Конкретные метрики**: калории, БЖУ, количество подходов, часы сна

### 💾 Автосохранение профиля
- **Локальное хранение**: профиль сохраняется автоматически после первой генерации
- **Быстрый старт**: при следующем открытии все данные уже заполнены
- **История рекомендаций**: автоматическое сохранение последних 20 планов

### 🎨 Современный UI/UX
- **Material Design**: карточки с тенями и плавными анимациями
- **Удобный ввод**: dropdown меню для выбора цели и уровня активности
- **Структурированный вывод**: результат разделен на удобные секции с иконками
- **Адаптивность**: работает на iOS и Android

---


## 🏗️ Архитектура проекта

```
AI-FITNESS-COACH/
│
├── backend/                    # FastAPI сервер
│   ├── main.py                # API endpoints + Groq интеграция
│   ├── requirements.txt       # Python зависимости
│   └── .env                   # API ключи (не в Git)
│
└── frontend/                  # React Native (Expo) приложение
    ├── app/
    │   ├── (tabs)/
    │   │   ├── index.tsx      # Главный экран (форма + результаты)
    │   │   ├── explore.tsx    # Экран "Explore" (пример)
    │   │   └── _layout.tsx    # Навигация табов
    │   ├── _layout.tsx        # Главный layout
    │   └── modal.tsx          # Модальное окно (пример)
    │
    ├── services/
    │   └── storage.ts         # Сервис локального хранения (AsyncStorage)
    │
    ├── components/            # Переиспользуемые UI компоненты
    ├── constants/             # Константы (цвета, темы)
    ├── hooks/                 # Custom React hooks
    └── assets/                # Картинки, иконки, шрифты
```

---

## 🚀 Быстрый старт

### Предварительные требования

- **Node.js** 18+ ([скачать](https://nodejs.org/))
- **Python** 3.9+ ([скачать](https://python.org/))
- **Groq API ключ** (бесплатный, получить на [console.groq.com](https://console.groq.com/))
- **Ngrok** ([скачать](https://ngrok.com/download)) - для доступа с телефона
- **Expo Go** на телефоне ([iOS](https://apps.apple.com/app/expo-go/id982107779) | [Android](https://play.google.com/store/apps/details?id=host.exp.exponent))

---

### 1️⃣ Клонирование репозитория

```bash
git clone https://github.com/Adilet-tech/AI-Fitness-Coach.git
cd AI-Fitness-Coach
```

---

### 2️⃣ Настройка Backend

```bash
cd backend

# Создать виртуальное окружение
python -m venv venv

# Активировать
# Mac/Linux:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt

# Создать .env файл
echo "GROQ_API_KEY=ваш_ключ_здесь" > .env

# Запустить сервер
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Сервер запустится на `http://localhost:8000`

Проверьте: откройте в браузере `http://localhost:8000/docs`

---

### 3️⃣ Запуск Ngrok (для доступа с телефона)

Откройте **новый терминал**:

```bash
ngrok http 8000
```

Скопируйте URL из строки `Forwarding`, например:
```
https://abc123-xyz.ngrok-free.app
```

---

### 4️⃣ Настройка Frontend

Откройте **третий терминал**:

```bash
cd frontend

# Установить зависимости
npm install

# Установить AsyncStorage
npx expo install @react-native-async-storage/async-storage
```

**Обновите API URL:**

Откройте `frontend/app/(tabs)/index.tsx` и измените строку 10:

```typescript
const API_URL = 'https://ваш-ngrok-url.ngrok-free.app'; // ← Вставьте ваш URL
```

---

### 5️⃣ Запуск приложения

```bash
npx expo start
```

Отсканируйте QR-код приложением **Expo Go** на телефоне.

---

## 🎯 Использование

1. **Заполните форму:**
   - Возраст (10-120 лет)
   - Вес (30-300 кг)
   - Рост (100-250 см)
   - Выберите цель из dropdown (похудеть, набрать массу и т.д.)
   - Выберите уровень активности

2. **Нажмите "✨ Получить план"**

3. **Получите персональные рекомендации:**
   - 🍽️ **Питание**: калории, БЖУ, примеры блюд
   - 💪 **Тренировки**: упражнения, подходы, частота
   - 😴 **Сон**: режим, восстановление

4. **Профиль сохранится автоматически** - при следующем открытии все данные уже заполнены!

---

## 🛠️ Технологии

### Backend
- **FastAPI** - современный Python web-фреймворк
- **Groq API** - сверхбыстрый AI inference
- **LLaMA 3.3 70B** - мощная языковая модель
- **Pydantic** - валидация данных
- **Python-dotenv** - управление переменными окружения

### Frontend
- **React Native** - кроссплатформенная разработка
- **Expo** - инструментарий для React Native
- **TypeScript** - типизация для JavaScript
- **AsyncStorage** - локальное хранилище данных
- **Animated API** - плавные анимации

### DevOps
- **Ngrok** - туннелирование для локальной разработки
- **Uvicorn** - ASGI сервер для FastAPI

---

## 📦 Что нового (последние обновления)

### v2.0 - Красивый UI + Автосохранение (текущая версия)

#### ✨ Новые возможности:
- **Автосохранение профиля**: данные сохраняются локально после первой генерации
- **История рекомендаций**: хранение последних 20 планов
- **Dropdown меню**: удобный выбор цели и уровня активности
- **Структурированный вывод**: результат разбит на красивые карточки
- **Плавные анимации**: появление результатов с fade-in эффектом
- **Улучшенный промпт**: AI дает более конкретные и измеримые рекомендации

#### 🎨 UI/UX улучшения:
- Современный Material Design
- Карточки с тенями и скругленными углами
- Иконки для каждого поля (👤 ⚖️ 📏 🎯 🏃)
- Цветовые акценты и градиенты
- Адаптивная верстка для разных размеров экранов
- Индикатор загрузки с текстом
- Визуальные подсказки (placeholder с примерами)

#### 🔧 Технические улучшения:
- Сервис `StorageService` для работы с локальным хранилищем
- Валидация данных на клиенте и сервере
- CORS middleware для безопасности
- Улучшенная обработка ошибок
- Оптимизация промпта для более структурированного ответа
- TypeScript типизация для всех компонентов

---

### v1.0 - MVP

#### ✅ Базовые функции:
- Генерация персональных рекомендаций
- Интеграция с Groq API
- Базовая форма ввода данных
- Простой текстовый вывод результата

---

## 📂 Структура файлов

### Backend (`backend/`)

```
backend/
├── main.py              # FastAPI приложение
│   ├── /generate-recommendation  # POST endpoint для генерации
│   ├── UserInput         # Pydantic модель для валидации
│   └── Groq client       # Интеграция с LLaMA 3.3 70B
│
├── requirements.txt     # Зависимости:
│   ├── fastapi==0.119.0
│   ├── groq==0.32.0
│   ├── uvicorn==0.37.0
│   ├── python-dotenv==1.1.1
│   └── pydantic==2.12.1
│
└── .env                 # API ключи (создать вручную)
    └── GROQ_API_KEY=...
```

### Frontend (`frontend/`)

```
frontend/
├── app/
│   ├── (tabs)/
│   │   ├── index.tsx           # Главный экран (530 строк)
│   │   │   ├── Форма ввода с dropdown
│   │   │   ├── Валидация
│   │   │   ├── API запросы
│   │   │   ├── Автосохранение
│   │   │   └── Рендеринг результата
│   │   │
│   │   ├── explore.tsx         # Экран "Explore"
│   │   └── _layout.tsx         # Tabs навигация
│   │
│   ├── _layout.tsx             # Root layout
│   └── modal.tsx               # Модальное окно
│
├── services/
│   └── storage.ts              # Сервис хранения (240 строк)
│       ├── saveProfile()
│       ├── loadProfile()
│       ├── deleteProfile()
│       ├── saveRecommendation()
│       └── loadRecommendationsHistory()
│
├── components/                 # UI компоненты
├── constants/                  # Константы
├── hooks/                      # Custom hooks
└── assets/                     # Медиа файлы
```

---

## 🔑 API Endpoints

### POST `/generate-recommendation`

Генерирует персональные рекомендации на основе данных пользователя.

**Request Body:**
```json
{
  "age": 25,
  "weight": 70.5,
  "height": 175,
  "goal": "похудеть",
  "activity_level": "средняя активность"
}
```

**Response:**
```json
{
  "recommendation": "### Питание\n\nВаш суточный рацион должен составлять 2000 ккал...\n\n### Тренировки\n\nРекомендую тренироваться 4 раза в неделю...\n\n### Сон\n\nОптимальная продолжительность сна - 7-8 часов..."
}
```

**Валидация:**
- `age`: 10-120
- `weight`: 30-300
- `height`: 100-250
- `goal`: любая строка
- `activity_level`: любая строка

---

## 💾 Локальное хранилище

### Сохраняемые данные:

#### Профиль пользователя (`@fitness_user_profile`)
```typescript
{
  age: number,
  weight: number,
  height: number,
  goal: string,
  activityLevel: string,
  lastUpdated: string
}
```

#### История рекомендаций (`@fitness_recommendations_history`)
```typescript
[
  {
    id: string,
    recommendation: string,
    profile: UserProfile,
    createdAt: string
  },
  // ... до 20 записей
]
```

### API методы:

```typescript
// Сохранить профиль
await StorageService.saveProfile(profileData);

// Загрузить профиль
const profile = await StorageService.loadProfile();

// Удалить профиль
await StorageService.deleteProfile();

// Сохранить рекомендацию
await StorageService.saveRecommendation(text, profile);

// Загрузить историю
const history = await StorageService.loadRecommendationsHistory();
```

---

## 🎨 UI Компоненты

### Форма ввода
- **TextInput** - для возраста, веса, роста
- **TouchableOpacity + Modal** - для цели и уровня активности
- **Dropdown меню** - список опций с иконками и галочками
- **Button** - генерация и очистка профиля

### Результат
- **Animated.View** - плавное появление
- **ScrollView** - прокрутка длинного текста
- **Карточки (View)** - для каждой секции (Питание, Тренировки, Сон)
- **Разделители** - визуальное разделение секций

### Индикаторы
- **ActivityIndicator** - загрузка
- **Badge** - "Профиль сохранен"
- **Alert** - уведомления об ошибках

---

## 🧪 Тестирование

### Проверка Backend:

```bash
# Открыть Swagger UI
http://localhost:8000/docs

# Попробовать endpoint
curl -X POST http://localhost:8000/generate-recommendation \
  -H "Content-Type: application/json" \
  -d '{"age":25,"weight":70,"height":175,"goal":"похудеть","activity_level":"активный"}'
```

### Проверка Frontend:

1. Заполнить форму
2. Нажать "Получить план"
3. Проверить что появился результат
4. Закрыть приложение
5. Открыть снова → данные должны быть заполнены

---

## 🐛 Известные проблемы и решения

### Проблема: "Ошибка сети"

**Причины:**
- Backend не запущен
- Ngrok не запущен
- Неправильный URL в index.tsx

**Решение:**
1. Проверьте что backend работает: `http://localhost:8000/docs`
2. Проверьте что ngrok работает и URL правильный
3. Обновите `API_URL` в index.tsx
4. Перезапустите Expo: `npx expo start -c`

---

### Проблема: Данные не сохраняются

**Решение:**
1. Проверьте что AsyncStorage установлен
2. Проверьте логи в консоли (должны быть "✅ Профиль сохранен")
3. Попробуйте очистить кеш: `npx expo start -c`

---

### Проблема: Результат не разбит на секции

**Решение:**
1. Обновите `backend/main.py` (улучшенный промпт)
2. Перезапустите backend
3. Сгенерируйте новый план

---

## 🔮 Планы на будущее

### v2.1 (ближайшие обновления)
- [ ] Экран истории рекомендаций
- [ ] Возможность отметить дни выполнения тренировок
- [ ] Экспорт плана в PDF
- [ ] Темная тема

### v2.2
- [ ] Онлайн синхронизация (Firebase/Supabase)
- [ ] Опциональная регистрация
- [ ] Социальные функции (поделиться результатом)

### v3.0
- [ ] Расширенная аналитика (графики прогресса)
- [ ] Интеграция с Apple Health / Google Fit
- [ ] Push-уведомления (напоминания о тренировках)
- [ ] Библиотека упражнений с видео
- [ ] Чат-бот для вопросов

---

## 🤝 Вклад в проект

Буду рад вашим Pull Request'ам!

### Как внести вклад:

1. Fork репозиторий
2. Создайте ветку: `git checkout -b feature/amazing-feature`
3. Commit изменения: `git commit -m 'Add amazing feature'`
4. Push в ветку: `git push origin feature/amazing-feature`
5. Откройте Pull Request

### Что можно улучшить:
- Добавить новые типы целей (сушка, набор силы, гибкость)
- Улучшить валидацию (проверка BMI, возрастные ограничения)
- Добавить локализацию (английский язык)
- Написать unit тесты
- Оптимизировать промпт для более точных рекомендаций

---

## 📄 Лицензия

MIT License - свободно используйте в своих проектах!

---

## 👨‍💻 Автор

**Adilet**
- GitHub: [@Adilet-tech](https://github.com/Adilet-tech)
- Проект: [AI-Fitness-Coach](https://github.com/Adilet-tech/AI-Fitness-Coach)

---

## 🙏 Благодарности

- **Groq** - за быстрый и бесплатный AI API
- **Meta AI** - за LLaMA 3.3 70B модель
- **Expo Team** - за отличный инструментарий для React Native
- **FastAPI** - за простоту и скорость разработки


---

<div align="center">

**Сделано с ❤️ 

⭐ Поставьте звезду, если проект вам понравился!

</div>